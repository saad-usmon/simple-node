#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-install-release/node_modules/node-semvers/bin/node-semvers.js" "$@"
  ret=$?
else 
  node  "$basedir/../node-install-release/node_modules/node-semvers/bin/node-semvers.js" "$@"
  ret=$?
fi
exit $ret
