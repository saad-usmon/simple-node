#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-install-release/bin/node-install-release.js" "$@"
  ret=$?
else 
  node  "$basedir/../node-install-release/bin/node-install-release.js" "$@"
  ret=$?
fi
exit $ret
